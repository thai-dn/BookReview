<% @books.each do |book| %>
  <div class="col-md-3">
    <%= link_to book_path(book) do  %>
      <%= image_tag (book.avatar_url), class: "book", title: book.title, alt: book.title %>
      <p> <%= book.title[0..30]%> </p>
    <% end %>
  </div>
<% end  %>